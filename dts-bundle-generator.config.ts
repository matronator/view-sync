const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/view-sync.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
